# Ship Images

Place your ship overhead image here.

**Required filename:** `ship-overhead.png`

This should be the top-down/bird's-eye view of the ship (like the one provided).
The system will scale it to fit the center panel width.
